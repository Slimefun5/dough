description=Dough is Baked Libs' core utility library, forked and lowered to Java 8 for the Slimefun multi-version universal jar. It is published as separate modules (see below).
example=```|// Dough utilities are used across Slimefun: config, items, inventories, scheduling, reflection, ...|// Pull every module at once with the github-gradle "all" classifier (see the Modules section).|```
