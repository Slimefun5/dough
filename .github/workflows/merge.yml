name: Merge

on:
  workflow_dispatch:
  push:
    branches:
      - development

permissions:
  contents: write

jobs:
  merge:
    uses: intisy/workflows/.github/workflows/merge.yml@main
    with:
      source: development
      target: main
      generated_paths: README.md
