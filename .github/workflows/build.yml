name: Build

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: intisy/workflows/.github/workflows/test.yml@main
    with:
      gradle_test: build
      gradle_args: --no-daemon
    secrets:
      envPAT: ${{ secrets.PAT }}
